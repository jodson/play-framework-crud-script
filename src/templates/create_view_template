@(form: Form[%clazz])

@main("Criar") {
  <div class="container">
    @helper.form(action = routes.%Controller.save()) {
      %inputs
      <p><input type="submit" value="Salvar" class="btn btn-info" /></p>
    }
  </div>
}